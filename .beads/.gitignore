# SQLite databases
*.db
*.db?*
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Legacy database files
db.sqlite
bd.db

# Merge artifacts (temporary files from 3-way merge)
beads.base.jsonl
beads.base.meta.json
beads.left.jsonl
beads.left.meta.json
beads.right.jsonl
beads.right.meta.json

# Data files - only tracked on beads-metadata branch
# Use 'bd sync' to commit these to beads-metadata
issues.jsonl
metadata.json
deletions.jsonl

# Keep config tracked on all branches
!config.yaml

# Local version tracking (not needed in git)
.local_version
bd.sock.startlock
