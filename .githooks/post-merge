#!/bin/bash
# Import beads changes after merge/pull
# This ensures local database is updated with any new issues from remote

if command -v bd &> /dev/null && [ -d ".beads" ]; then
    bd sync --quiet 2>/dev/null || true
fi
